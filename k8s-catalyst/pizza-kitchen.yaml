apiVersion: apps/v1
kind: Deployment
metadata:
  name: pizza-kitchen-deployment
spec:
  selector:
    matchLabels:
      app: pizza-kitchen-service
  template:
    metadata:
      labels:
        app: pizza-kitchen-service
        app.kubernetes.io/name: pizza-kitchen-service
        app.kubernetes.io/part-of: pizza-kitchen-service
        app.kubernetes.io/version: 0.1.0
    spec:
      containers:
      - name: pizza-kitchen-service
        image: salaboy/pizza-kitchen:1.0.0-agentic-amd64
        imagePullPolicy: Always
        env:
        - name: SERVER_PORT
          value: "8080"
        - name: JAVA_OPTS
          value: "-XX:+UseParallelGC -XX:ActiveProcessorCount=1 -XX:MaxRAMPercentage=75 -XX:TieredStopAtLevel=1"   
        - name: PUB_SUB_NAME
          value: pubsub       
        - name: PUB_SUB_TOPIC
          value: topic  
        - name: DAPR_HTTP_ENDPOINT
          value: "<YOUR CATALYST PROJECT DAPR HTTP ENDPOINT HERE>"
        - name: DAPR_GRPC_ENDPOINT
          value: "<YOUR CATALYST PROJECT DAPR GRPC ENDPOINT HERE>"
        - name: DAPR_API_TOKEN
          value: "<YOUR CATALYST STORE APPID API KEY HERE>"  
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8080
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8080
        resources:
          limits:
            cpu: "1"
            memory: "2Gi"
          requests:
            cpu: "1"
            memory: "2Gi"
        ports:
        - containerPort: 8080